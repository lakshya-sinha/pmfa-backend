<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Hacker-style Login</title>
    <style>
      /* Simple blue & black hacker-style login (no fancy libs) */
      :root {
        --bg: #000;
        --panel: #05060a;
        --accent: #0ea5ff; /* bright blue */
        --muted: #091321;
        --glass: rgba(14, 165, 255, 0.06);
        --mono: "SFMono-Regular", Menlo, Monaco, monospace;
      }
      html,
      body {
        height: 100%;
      }
      body {
        margin: 0;
        font-family: Inter, system-ui, sans-serif;
        background: linear-gradient(180deg, #000 0%, #020617 50%, #001026 100%);
        color: #cfefff;
        display: flex;
        align-items: center;
        justify-content: center;
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
        overflow: hidden;
      }

      /* subtle animated scanlines (blue) */
      .scanlines {
        position: fixed;
        inset: 0;
        pointer-events: none;
        background-image: linear-gradient(
          transparent 92%,
          rgba(14, 165, 255, 0.02) 100%
        );
        background-size: 100% 6px;
        mix-blend-mode: screen;
        opacity: 0.6;
        animation: drift 8s linear infinite;
      }
      @keyframes drift {
        from {
          transform: translateY(0);
        }
        to {
          transform: translateY(6px);
        }
      }

      /* centered panel */
      .panel {
        width: 360px;
        max-width: 92vw;
        padding: 28px;
        border-radius: 8px;
        background: linear-gradient(
          180deg,
          rgba(4, 6, 12, 0.8),
          rgba(3, 5, 10, 0.65)
        );
        box-shadow: 0 8px 30px rgba(2, 6, 15, 0.8),
          inset 0 1px 0 rgba(255, 255, 255, 0.02);
        border: 1px solid rgba(14, 165, 255, 0.08);
        position: relative;
        z-index: 2;
      }

      .brand {
        display: flex;
        align-items: center;
        gap: 10px;
        margin-bottom: 18px;
      }
      .dot {
        width: 12px;
        height: 12px;
        border-radius: 50%;
        background: var(--accent);
        box-shadow: 0 0 10px rgba(14, 165, 255, 0.5);
      }
      .title {
        font-family: var(--mono);
        font-size: 16px;
        letter-spacing: 1px;
        color: var(--accent);
      }
      .sub {
        font-size: 12px;
        color: #9fdfff;
        opacity: 0.75;
      }

      form {
        display: flex;
        flex-direction: column;
        gap: 12px;
      }

      .field {
        position: relative;
      }
      label {
        font-size: 11px;
        color: #9fdfff;
        letter-spacing: 0.6px;
        margin-bottom: 6px;
        display: block;
      }
      input[type="text"],
      input[type="password"] {
        width: 100%;
        padding: 12px 12px;
        border-radius: 6px;
        border: 1px solid rgba(14, 165, 255, 0.12);
        background: linear-gradient(
          180deg,
          rgba(6, 10, 18, 0.6),
          rgba(3, 6, 12, 0.4)
        );
        color: #e6f9ff;
        font-family: var(--mono);
        font-size: 13px;
        outline: none;
        box-sizing: border-box;
        transition: box-shadow 0.15s, border-color 0.15s, transform 0.06s;
      }
      input::placeholder {
        color: rgba(159, 223, 255, 0.25);
        font-family: var(--mono);
      }

      input:focus {
        box-shadow: 0 0 18px rgba(14, 165, 255, 0.08);
        border-color: var(--accent);
        transform: translateY(-1px);
      }

      .actions {
        display: flex;
        align-items: center;
        justify-content: space-between;
        margin-top: 6px;
      }
      .btn {
        background: transparent;
        border: 1px solid var(--accent);
        padding: 10px 14px;
        border-radius: 6px;
        color: var(--accent);
        font-family: var(--mono);
        font-weight: 600;
        letter-spacing: 0.6px;
        cursor: pointer;
      }
      .btn.secondary {
        opacity: 0.6;
        border-color: rgba(14, 165, 255, 0.06);
        color: #9fdfff;
      }

      .status {
        font-family: var(--mono);
        font-size: 12px;
        color: #9fdfff;
        opacity: 0.9;
      }

      /* dynamic log row */
      .logbox {
        margin-top: 14px;
        background: rgba(6, 12, 20, 0.5);
        padding: 10px;
        border-radius: 6px;
        font-family: var(--mono);
        font-size: 12px;
        color: #9fdfff;
        height: 96px;
        overflow: auto;
        border: 1px solid rgba(14, 165, 255, 0.04);
      }

      /* small terminal cursor effect for placeholders */
      .typehint {
        display: inline-block;
      }
      .caret {
        display: inline-block;
        width: 8px;
        height: 14px;
        background: var(--accent);
        margin-left: 6px;
        vertical-align: middle;
        animation: blink 1s steps(2) infinite;
      }
      @keyframes blink {
        50% {
          opacity: 0;
        }
      }

      /* error shake */
      .shake {
        animation: shake 0.35s ease-in-out;
      }
      @keyframes shake {
        10%,
        90% {
          transform: translateX(-3px);
        }
        20%,
        80% {
          transform: translateX(4px);
        }
        30%,
        50%,
        70% {
          transform: translateX(-6px);
        }
        40%,
        60% {
          transform: translateX(6px);
        }
      }

      /* footer hints */
      .hint {
        margin-top: 12px;
        font-size: 12px;
        color: rgba(159, 223, 255, 0.6);
        display: flex;
        gap: 8px;
        align-items: center;
      }
      .hint .k {
        background: var(--muted);
        padding: 6px 8px;
        border-radius: 4px;
        border: 1px solid rgba(14, 165, 255, 0.04);
        font-family: var(--mono);
        font-size: 11px;
      }

      /* small responsive */
      @media (max-width: 420px) {
        .panel {
          padding: 18px;
          width: 92vw;
        }
      }
    </style>
  </head>
  <body>
    <div class="scanlines" aria-hidden="true"></div>

    <main class="panel" id="panel">
      <div class="brand">
        <div class="dot" aria-hidden></div>
        <div>
          <div class="title">PLAYMAKER FA. - ADMIN</div>
          <div class="sub">login</div>
        </div>
      </div>

      <form id="loginForm" autocomplete="off" action="/login" method="post">
        <div class="field">
          <label for="user">USERNAME</label>
          <input
            id="user"
            name="user"
            type="text"
            placeholder="enter handle"
            autocomplete="off"
            autocapitalize="off"
            spellcheck="false"
          />
        </div>

        <div class="field">
          <label for="pass">PASSWORD</label>
          <input
            id="pass"
            name="password"
            type="password"
            placeholder="••••••••"
          />
        </div>
        <button class="btn" type="submit">Submit</button>
      </form>
    </main>
  </body>
</html>
